# Paseo Workflow Starter Guide

Small static website that teaches a new developer how to start using `paseo-workflow`.

- Only HTML, CSS, vanilla JavaScript. No build, no dependencies.
- Files: `index.html`, `styles.css`, `app.js`
- Open locally: `python3 -m http.server 8000` then visit http://localhost:8000
- Source of truth: `/home/audition/projects/personal/paseo-workflow` (README, AGENTS.md, `docs/specs/2026-09-24-paseo-workflow-bootstrap-v1.md`)
- Status disclaimer: upstream repo is SPECIFICATION / DESIGN COMPLETE; `install.sh` / `verify.sh` are accepted TARGET, not shipped. Never claim OPERATIONALLY VALIDATED without Preflight + A–G evidence.
